# Workflow này không còn cần thiết.
# Việc đăng bài được thực hiện trực tiếp trong scrape.yml
# (upload lên Facebook dạng scheduled post ngay khi scrape)
name: Post Scheduled Facebook Posts

on:
  workflow_dispatch:  # Chỉ chạy thủ công nếu cần, không schedule tự động

jobs:
  post:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Thông báo
        run: echo "Workflow này đã được thay thế bởi scrape.yml"
